function solution(n, left, right) {
    // 1234 2234 3334 4444
    // 123 223 333 
    // 답 보고서 이해함. 좌표로 해서 (a,b) a,b중에서 큰수 +1 하면됨
    // a,b좌표로 바꾸는법은 n으로 나눈 몫과 나머지로 설정
//    const ans = [];

//     while (left <= right) {
//         ans.push(Math.max(Math.floor(left / n), left++ % n) + 1);
//     }

//     return ans;
}
